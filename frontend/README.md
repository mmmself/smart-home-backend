# Smart Home Frontend

智能家居系统前端，基于 React + Vite + TailwindCSS。

## 开发

**前置条件:** Node.js

1. 安装依赖:
   `npm install`
2. 复制环境变量文件:
   `cp .env.example .env.local`
3. 启动开发服务器:
   `npm run dev`

## 构建

`npm run build`
